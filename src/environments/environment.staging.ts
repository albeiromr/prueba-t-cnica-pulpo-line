//este es el environment que se usa para desplegar al ambiente de pruebas o staging, no usar para producción
export const environment = {
    name: "staging",
    production: false,
    backendHost: "https://jsonplaceholder.typicode.com"
};
